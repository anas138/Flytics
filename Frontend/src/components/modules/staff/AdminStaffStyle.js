const styles = (theme) => ({
  buttonContainer: {
    display: "flex",
    justifyContent: "space-between",
    alignItem: "center",
  },
  backButton: {
    cursor: "pointer",
  },
  flexClass:{
   textAlign:"center",
  }
});
export default styles;
