const styles = (theme) => ({
  unViewedAlert: {
    "&.MuiListItem-root": {
      background: `linear-gradient(to right,rgba(157,157,157,0.025),rgba(70,70,70,0.025))`,
      color: "black",
      fontWeight: "bold",
    },
  },
});

export default styles;
